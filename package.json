{
  "name": "uc-inventory",
  "version": "2.0.0",
  "private": true,
  "description": "UC deployment management console — single-service deploy (backend serves the built SPA)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install && npm --prefix backend run build",
    "start": "npm --prefix backend start"
  }
}
